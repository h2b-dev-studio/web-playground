{
  "name": "preact-sample",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rsbuild dev",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "preact": "^10.23.1"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.1.0",
    "@rsbuild/plugin-preact": "^1.0.0",
    "typescript": "^5.5.4"
  }
}
