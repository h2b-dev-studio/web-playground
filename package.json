{
  "name": "web-playground",
  "version": "1.0.0",
  "description": "웹 기술을 탐색하고 실험하기 위한 개인 공간입니다.",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "build:entry": "pnpm --filter entry run build",
    "build:react-sample": "pnpm --filter react-sample run build",
    "dev": "pnpm -r run dev",
    "dev:entry": "pnpm --filter entry run dev",
    "dev:react-sample": "pnpm --filter react-sample run dev",
    "clean": "rm -rf dist && pnpm -r run clean",
    "test": "pnpm -r run test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:report": "playwright show-report",
    "test:e2e:codegen": "playwright codegen http://localhost:5173",
    "check:spec": "tsx scripts/check-spec-compliance.ts",
    "prepare": "husky"
  },
  "keywords": [
    "monorepo",
    "entry",
    "react-sample"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.19.0",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "husky": "^9.1.7",
    "tsx": "^4.16.2"
  }
}
